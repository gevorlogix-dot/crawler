[pytest]
testpaths = tests
addopts = -v --strict-markers --tb=short
markers =
    smoke: core acceptance checks
    seo: SEO / technical-SEO assertions
    unit: pure-function tests for the audit engine (no browser, no network)
filterwarnings =
    ignore::DeprecationWarning
